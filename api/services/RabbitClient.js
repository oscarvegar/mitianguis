 // EmailService.js - in api/services

//var context = require('rabbit.js').createContext("amqp://superpower:powersuper@mitianguis.mx");
//var pub = context.socket('PUSH');

module.exports = {

/*
    publish: function(msg,queueName){
    	pub.connect(queueName, function() {
    		pub.write(JSON.stringify(msg), 'utf8');
    	});


    }
    */
};
